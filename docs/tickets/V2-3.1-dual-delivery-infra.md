# Ticket V2-3.1 — Dual Delivery Deployment Topologies (Cloud Run REST + GCE VM SQL API)

| | |
|---|---|
| **ID** | V2-3.1 |
| **Type** | platform |
| **Milestone** | Milestone 3 — Dual Delivery Infrastructure & Consumer Integration |
| **Status** | DONE |
| **Blocked On** | — |
| **Touches** | `version-two/infra/Dockerfile`, `version-two/infra/cloudrun-rest.yaml`, `version-two/infra/gce-sql-systemd.service` |

---

## Goal
Author production deployment manifests for the Dual Delivery Architecture (ADR-0004), producing a unified Docker image deployed to Google Cloud Run for REST API (port 4000) and to a Compute Engine VM via systemd for SQL API (port 5432).

## Acceptance Criteria
1. Production multi-stage `Dockerfile` packaging Node 22, Cube 1.7.x, and embedded DuckDB.
2. Cloud Run YAML deployment manifest configured with VPC Access Connector and Secret Manager environment variables.
3. Systemd unit service file and startup script for `scbi-cube-sql` Compute Engine VM.
4. Confirms both services start cleanly from the identical Docker image with appropriate runtime flags.

## User Stories
1. *As a platform DevOps engineer*, I want a single Docker image serving both REST and SQL workloads, so that CI/CD builds only one artifact and eliminates version drift.
2. *As a network engineer*, I want the SQL VM isolated inside internal VPC without a public IP, so that access is restricted to authenticated IAP tunnels and peered services.

## Seam
Local container smoke test verifying that the built Docker image successfully responds on port 4000 in REST mode and port 5432 in SQL mode.
